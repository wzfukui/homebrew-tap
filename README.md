# Homebrew Tap for psmore

This repository hosts the Homebrew formula for:

- psmore - relationship-first process diagnostics workbench

Usage:

```bash
brew tap wzfukui/tap
brew install psmore
```

Formula source:

- https://github.com/wzfukui/psmore
- Version: `v0.1.0`
